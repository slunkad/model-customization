 I am a product manager at a financial institution working on the AI platform team and I am working on the M2 model customization workflow now to give you some context the across the organization the engineering teams can be divided into three groups one hour ML engineers who have a lot of experience with AI ML infrastructure second group is data, analyst and data scientists who can play around with models, but cannot work on infrastructure and the third group is engineers and application developers who have a lot of strong technical background, but don't have a lot of ML expertise and if you look at the ratio, there is higher number of application, developers and engineers across the firm as compared to a scientist, and there is an overall push across the organization to build AI AI use cases across the firm so everybody is trying to customize models everybody's trying to come up with use cases now for this specific group of people we released some user guys and recipes to find tune models, but we got we got good response from users, trying to fine tune, but a lot of the questions that came to us were very very, very simple very basic ML Python related questions that any ML engineer would know which made us realize that a lot of these folks don't quite understand ML infrastructure but still will want to fine-tune or customize models to use cases so for the specific group of people we want to make this process easy and abstract away all of the plumbing code that is behind customizing or fine tuning models with with this in mind, I want to build an end workflow for this entire process, which starts with getting the maze model from our catalogue, then customizing it now customizing can be invasive or less invasive more invasive so the less invasive way are prompt, customization, or context enginee and then having or and the more invasive way is fine tuning, which will under fine tuning. I want to cover all the different types of Q lora and qlora quantization SFT RLHF DPO all of that stuff and instead of having to build all of these scripts from scratch and having this complexity and dependency on our engineering team I want to leverage a lot of these open source libraries or some of these tools like factory or axolotl, which provides an interface to make this fine-tuning process easier and outsource basically some of this plumbing code so that from the user from the user perspective, they are only entering the confits for example, if they're working on Laura, they only enter the rank or the alpha or some of the other Confits and don't have to really write the plumbing code so in terms of user experience, the user comes in they bring the base model they decide to do fine-tuning they decide to pick a method for example lora there is a intuitive UI to select whatever config they want to select and they click. They provide the data set and they click on and the platform should essentially run a fine-tuning job. Give the logs and come notify the user once the job is done, same with prompt customization, I want to provide like an intuitive way where the user can just bring the model in there is a UI for the user to prov prompt change prompt and then there is a playground where we can immediately test the model on different prompts so then there is a input for prompt and then there is a run button where the user can immediately immediately see the customized model response so post customization assuming that this is all in place we also want to provide inference capabilities where whatever customized model the user has built, we want to provide a evaluation dataset which has some prompt or possibly ground truth and we want to generate the customized model responses across these test pros and then all of the all of the complexity of distributor training batching all of that stuff is abstracted away from the users. The user have just have to provide the data set and click on and get the model responses from their customized model and then the next step in this process would be evaluation because there is no way for the users to know whether the fine model is working well or not just based on a few prompts so we want to provide an evaluation interface where the users in the on based on the inference provide like in the previous step, we have a big catalogue of matrix. We have a big catalogue of LM judges of programmatic matrix of all of the other scorers that are available for evaluation, and that is easy for users to immediately evaluate their model across the the eval data asset, and then get like a summary statistics of the metrics of insights on the model errors all of and essentially we want to provide this whole to end workflow so that the user just comes in with the motive of this is my use case I want to do XYZ I want to customize my LLM and can leave the platform with a model which is performing according to expectation thank you.